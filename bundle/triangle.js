"use strict";
function outputOfTriangle(a, b) {
    const output = 180 - (a + b);
    return output;
}
const a = 80;
const b = 65;
const result = outputOfTriangle(a, b);
console.log("angle triangle c =", result);
